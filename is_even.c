#include <stdio.h>      

int main()
{   
    int myNumber = 1111;

    if (myNumber % 2 == 0) {
        printf("%d is is even \n", myNumber);
    } else {
        printf("%d is is odd \n", myNumber);
    }

    return 0;
}